    var inviteCode = $("input[name='inviteCode']");
    var nickName = $("input[name='nickName']"); 

    /*check isUndefine*/
    var isUndefine = function(e){
        if(e == undefined){
            return true;
        }
        return false;
    };

    /*check isNull*/
    var isNull = function(e){
       if(e == ''){
           return true;
       }
        return false;
    };

    /*handle inviteCode input focus*/
    inviteCode.focus(function(){
        $('.error-message').text('');
    });

    /*handle nickName input focus*/
    nickName.focus(function(){
        $('.error-message').text('');
    });

	// loading重试层点击重试，重新发送请求
	$("#retry-btn").click(function(){
		// ajax发送请求
		ajax();
	})	
	// loading重试层点击返回，跳转到登录页
	$("#back-btn").click(function(){
		$(".load-layout-fail").hide();
		window.location.reload();
	})

    // 登录页确定按钮事件		
	$('#joinID').click(function(){
		loginClickEvent();
    });
    